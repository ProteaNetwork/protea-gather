// import { selectProfileContainerDomain } from '../selectors';

describe('selectProfileContainerDomain', () => {
  it('Expect to have unit tests specified', () => {
    expect(true).toEqual(false);
  });
});

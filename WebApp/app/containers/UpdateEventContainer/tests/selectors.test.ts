// import { selectViewEventContainerDomain } from '../selectors';

describe('selectViewEventContainerDomain', () => {
  it('Expect to have unit tests specified', () => {
    expect(true).toEqual(false);
  });
});

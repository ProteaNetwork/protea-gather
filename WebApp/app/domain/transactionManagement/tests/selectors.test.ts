// import { selectTransactionManagementDomain } from '../selectors';

describe('selectTransactionManagementDomain', () => {
  it('Expect to have unit tests specified', () => {
    expect(true).toEqual(false);
  });
});

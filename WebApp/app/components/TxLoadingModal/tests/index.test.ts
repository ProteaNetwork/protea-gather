// import React from 'react';
// import { mount } from 'enzyme';

// import TxLoadingModal from '../index';

describe('<TxLoadingModal />', () => {
  it('Expect to have unit tests specified', () => {
    expect(true).toEqual(false);
  });
});

// import React from 'react';
// import { mount } from 'enzyme';

// import CreateCommunityForm from '../index';

describe('<CreateCommunityForm />', () => {
  it('Expect to have unit tests specified', () => {
    expect(true).toEqual(false);
  });
});

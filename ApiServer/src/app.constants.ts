export enum Schemas {
  User = 'User',
  Community = 'Community',
  Event = 'Event',
  Attachment = 'Attachment',
  Error = 'Error'
}

export enum Modules {
  Logger = 'winston',
  EthersProvider = 'EthersProvider',
}

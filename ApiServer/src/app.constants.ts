export enum Schemas {
  User = 'User',
  Community = 'Community',
  Event = 'Event',
  Attachment = 'Attachment',
}

export enum Modules {
  Logger = 'winston',
  EthersProvider = 'EthersProvider',
}

export enum Schemas {
  User = 'User',
  Community = 'Community',
  Event = 'Event',
  Attachment = 'Attachment',
  Error = 'Error',
  Feedback = 'Feedback'
}

export enum Modules {
  Logger = 'winston',
  EthersProvider = 'EthersProvider',
}

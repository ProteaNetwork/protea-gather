/**
 *
 * CommunityCard
 *
 */

import * as React from 'react';

// import styled from 'styles/styled-components';


interface OwnProps {
  name: string;
  tokens: number;
  logo: string;
  id: string;
}

const CommunityCard: React.SFC<OwnProps> = (props: OwnProps) => {
  return (
    <div>
      
    </div>
  );
};

export default CommunityCard;

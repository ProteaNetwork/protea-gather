// import { selectCommunitiesPageDomain } from '../selectors';

describe('selectCommunitiesPageDomain', () => {
  it('Expect to have unit tests specified', () => {
    expect(true).toEqual(false);
  });
});
